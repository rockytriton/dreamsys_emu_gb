#pragma once

#include <cstdint>
#include <iostream>
#include <string>
#include <vector>

using std::string;
using std::vector;
using std::cout;
using std::endl;

typedef uint8_t byte; 
typedef uint16_t ushort;

ushort toShort(byte a, byte b);
